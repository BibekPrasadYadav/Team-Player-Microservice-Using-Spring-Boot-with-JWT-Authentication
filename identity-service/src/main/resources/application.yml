spring:
  datasource:
    url: jdbc:mysql://localhost:3306/identitydb?autoReconnect=true&useSSL=false
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect


  application:
    name: IDENTITY-SERVICE


server:
  port: 9006
